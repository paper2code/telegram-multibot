package plugins

/*

import (
	"gopkg.in/telegram-bot-api.v4"
)

// Plugin is a main interface for plugins
type Plugin interface {
	GetName() string
	GetDescription() string
	GetCommands() []string
	GetKeyboard() *tgbotapi.ReplyKeyboardMarkup
	UpdateHandler(update tgbotapi.Update) error
}
*/
